{
  "name": "@tardus/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for the TARDUS privacy payment protocol on Solana. Phantom/Solflare-targetable. Implements sealed-box AEAD (ed25519 → X25519 + HKDF-SHA-256 + ChaCha20-Poly1305), BIP-39 receiving-identity derivation, and tardus:// invoice URI codec.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "license": "SEE LICENSE IN ../LICENSE",
  "private": true,
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "@noble/ciphers": "^1.0.0",
    "@scure/bip39": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22"
  }
}
